package com.loadify.security;

/**
 * This package is intentionally kept for future JWT/RBAC expansion.
 * The current college-evaluation build follows the final user instruction:
 * no JWT and no RBAC, while passwords are still stored with BCrypt.
 */
public final class SecurityNotes {
    private SecurityNotes() {
    }
}
